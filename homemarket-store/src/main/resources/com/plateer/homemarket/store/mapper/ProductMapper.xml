<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plateer.homemarket.store.mapper.ProductMapper">

<select id="retrieveAll" resultType="com.plateer.homemarket.entity.Product">
	select 	product_id as productId,
			product_name as productName,
			categories,
			quantity,
			price,
			member_id as memberId,
			upload_date as uploadDate
	from product
</select>

<select id="retrieveByCategories" parameterType="String" resultType="com.plateer.homemarket.entity.Product">
	select 	product_id as productId,
			product_name as productName,
			categories,
			quantity,
			price,
			member_id as memberId,
			upload_date as uploadDate
	from product
	where categories = #{categories}
</select>

<select id="retrieveByName" parameterType="String" resultType="com.plateer.homemarket.entity.Product">
	select 	product_id as productId,
			product_name as productName,
			categories,
			quantity,
			price,
			member_id as memberId,
			upload_date as uploadDate
	from product
	where product_name like concat('%',#{productName},'%')
</select>

<select id="retrieve" parameterType="int" resultType="com.plateer.homemarket.entity.Product">
	select 	product_id as productId,
			product_name as productName,
			product_detail as productDetail,
			categories,
			quantity,
			price,
			member_id as memberId,
			upload_date as uploadDate
	from product
	where product_id = #{productId}
</select>
</mapper>